/**
 * WordPress dependencies
 */
import { withFilters } from '@wordpress/components'

const TopActionablePosts = () => null

export default withFilters( 'rankMath.analytics.topActionablePosts' )( TopActionablePosts )
