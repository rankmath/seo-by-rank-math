/**
 * WordPress dependencies
 */
import { withFilters } from '@wordpress/components'

const KeywordGraph = () => null

export default withFilters( 'rankMath.analytics.keywordStatsGraph' )( KeywordGraph )
