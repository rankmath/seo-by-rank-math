/**
 * WordPress dependencies
 */
import { withFilters } from '@wordpress/components'

const ReportButton = () => null

export default withFilters( 'rankMath.analytics.reportButton' )( ReportButton )
