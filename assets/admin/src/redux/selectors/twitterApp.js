/**
 * Get twitter app description.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app description.
 */
export function getTwitterAppDescription( state ) {
	return state.appData.twitterAppDescription
}

/**
 * Get twitter app iphone id.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app iphone id.
 */
export function getTwitterAppIphoneID( state ) {
	return state.appData.twitterAppIphoneID
}

/**
 * Get twitter app iphone name.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app iphone name.
 */
export function getTwitterAppIphoneName( state ) {
	return state.appData.twitterAppIphoneName
}

/**
 * Get twitter app iphone url.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app iphone url.
 */
export function getTwitterAppIphoneUrl( state ) {
	return state.appData.twitterAppIphoneUrl
}

/**
 * Get twitter app ipad id.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app ipad id.
 */
export function getTwitterAppIpadID( state ) {
	return state.appData.twitterAppIpadID
}

/**
 * Get twitter app ipad name.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app ipad name.
 */
export function getTwitterAppIpadName( state ) {
	return state.appData.twitterAppIpadName
}

/**
 * Get twitter app ipad url.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app ipad url.
 */
export function getTwitterAppIpadUrl( state ) {
	return state.appData.twitterAppIpadUrl
}

/**
 * Get twitter app google play id.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app google play id.
 */
export function getTwitterAppGoogleplayID( state ) {
	return state.appData.twitterAppGoogleplayID
}

/**
 * Get twitter app google play name.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app google play name.
 */
export function getTwitterAppGoogleplayName( state ) {
	return state.appData.twitterAppGoogleplayName
}

/**
 * Get twitter app google play url.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app google play url.
 */
export function getTwitterAppGoogleplayUrl( state ) {
	return state.appData.twitterAppGoogleplayUrl
}

/**
 * Get twitter app country.
 *
 * @param {Object} state The app state.
 *
 * @return {string} Return twitter app country.
 */
export function getTwitterAppCountry( state ) {
	return state.appData.twitterAppCountry
}
