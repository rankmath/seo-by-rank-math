/**
 * Internal Dependencies
 */
import urlSubmission from './url-submission'
import settings from './settings'
import history from './history'

export default {
	'url-submission': urlSubmission,
	settings,
	history,
}
